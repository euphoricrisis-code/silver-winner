services:
  - type: web
    name: nvidia-nim-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
